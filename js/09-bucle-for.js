'use strict'

/**Se evalua la condición y se seguirá ejecutando a menos que se cumpla, en este caso es
 * menor o igual a 100, si llega a los 101 se detiene y sale del bucle
 */

for (var i = 0; i <= 100; i++){
    console.log('El numero' +i);
    //esta es una forma para incrementar (sino se coloca se crea un bucle infinito)
    // i=i+1
}