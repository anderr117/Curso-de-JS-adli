'use strict';

/**
 * Ejercicio 57
 * Evalúa true && false.
 */

function comparacionAndLogico() {
    // El primer operando (true) es "truthy", por lo que && evalúa y devuelve el segundo operando (false)
    console.log('true && false -> da:', true && false); // Imprime: false
}

comparacionAndLogico();