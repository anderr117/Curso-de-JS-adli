'use strict';

/**
 * Ejercicio 56
 * Evalúa true && true. ¿Qué devuelve?
 */

function comparacionBooleana() {

    // El primer operand (true) es verdadero, por lo que && devuelve el segundo operando (true)
    console.log('true && true -> da:', true && true); // Imprime: true

}

comparacionBooleana();