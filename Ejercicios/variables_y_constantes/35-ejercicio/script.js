'use strict'

/**
 * Ejercicio 35
 * Explica con tus palabras por qué es mejor usar let y const que var en términos de ámbito.
 */

/**
 * Es mejor usar let porque una vez declarada no se puede redeclarar, además tiene ambito de bloque,
 * solo es accesible dentro del bloque en donde se declaró la variable.
 * Var se puede redeclarar, esto puede causar confusiones e inconsistencias.
 */